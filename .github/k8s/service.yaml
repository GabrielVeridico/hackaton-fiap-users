apiVersion: v1
kind: Service
metadata:
  name: hackatonfiap-users-svc
  namespace: conexao-solidaria
  labels:
    app: hackatonfiap-users
spec:
  selector:
    app: hackatonfiap-users
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
